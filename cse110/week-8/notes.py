# len = grabs number of letters in word OR item in string`          `e37ikp\

# range = automatically creates a list of numbers based on integer inside function

# i = 'index', targets individual characters, goes down the list

# rand(.choice)

# when to put a variable up top or within functions:
#   - variable has to be defined before being used in a while condition

# range in a function: range(starts, ends, counts-by)